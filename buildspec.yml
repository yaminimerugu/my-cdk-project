version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
      - npm run build
  build:
    commands:
      - cdk synth
artifacts:
  files:
    - '**/*'
